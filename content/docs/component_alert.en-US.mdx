---
title: Alert
path: alert
publishedAt: 2024-02-09
githubLink: https://github.com/PolGubau/pol-ui/tree/main/src/components/Alert
doc_type: component
summary: Alerts are used to show a contextual text to the user such as a success or error message after form validation inside an alert box where you can choose from multiple colors, sizes, and styles.
pageLink: https://github.com/PolGubau/pol-ui/tree/main/src/components/Alert
tags:
  - Alert
  - React
  - Tailwind CSS
  - Pol-ui
---

import RootCode from "examples/Alert/root.mdx";
import root from "examples/Alert/root";

import OnDismissCode from "examples/Alert/onDismiss.mdx";
import OnDismiss from "examples/Alert/onDismiss";

import WithIcon from "examples/Alert/withIcon.mdx";
import WithIconCode from "examples/Alert/withIcon";

import Bordered from "examples/Alert/bordered.mdx";
import BorderedCode from "examples/Alert/bordered";

import Additional from "examples/Alert/bordered.mdx";
import AdditionalCode from "examples/Alert/bordered";

import Complete from "examples/Alert/complete.mdx";
import CompleteCode from "examples/Alert/complete";

import Theme from "examples/Alert/theme.mdx";

## Introduction

The alert component can be used to show a contextual text to the user such as a success or error message after form validation inside an alert box where you can choose from multiple colors, sizes, and styles.

The examples below are styled with Tailwind CSS and the reactivity is handled by React and you can also add any type of content inside the alert box.

To start using the alert box you need to import the **{"<Alert>"}** component from the **pol-ui** package:

```jsx
import { Alert } from "pol-ui";
```

## Default alert

The default alert component is a simple alert box with a text inside it and you can use the **color** prop to change the color of the alert box and the **title** prop to add a title to the alert box.

Inside of the **{"<Alert>"}** component you can add any type of content such as text, images, or other components as they will be considered children of the alert box.

<Example
  height={150}
name="Default alert"
code={root}
storybookUrl="alert--default"
embedUrl="alert--default&viewMode=story"

>

  <RootCode />
</Example>

## Props

| Prop name         | Type            | Default     | Description                                        |
| ----------------- | --------------- | ----------- | -------------------------------------------------- |
| icon              | React.ReactNode | null        | The icon to display in the accordion panel.        |
| onDismiss         | () => void      | null        | Callback to be called when the alert is dismissed. |
| children          | React.ReactNode | null        | The content to display in the alert.               |
| additionalContent | React.ReactNode | null        | Additional content to display in the alert.        |
| color             | Colors          | "secondary" | The color of the alert.                            |
| rounded           | RoundedEnum     | 'md'        | The border radius of the alert.                    |
| theme             | AlertTheme      | alertTheme  | The theme of the alert.                            |
| bordered          | boolean         | false       | Whether to add a border accent to the alert.       |

## Examples

### OnDismiss

You can use the **onDismiss** prop to add a close button to the alert box and a callback to be called when the alert is dismissed.

Any function can be passed to the **onDismiss** prop and it will be called when the alert is dismissed.

<Example
  height={150}
  name="Always open example"
  code={OnDismiss}
  storybookUrl="alert--alert-dismissible"
embedUrl="alert--alert-dismissible&viewMode=story"

>

  <OnDismissCode />
</Example>

### With icon

You can use the **icon** prop to add an icon to the alert box.
As usual, you can provide any time of svg as a React component to the **icon** prop.

<Example
  name="Alert with icon"   
  height={150}
code={WithIconCode} 
storybookUrl="alert--alert-with-icons"
embedUrl="alert--alert-with-icons&viewMode=story"

>

  <WithIcon />
</Example>

### Bordered

You can use the **icon** prop to add an icon to the alert box.
As usual, you can provide any time of svg as a React component to the **icon** prop.

<Example
  name="Alert Bordered"   
  height={150}
code={BorderedCode}
embedUrl="alert--alertbordered&viewMode=story"
storybookUrl="alert--alertbordered"

>

  <Bordered />
</Example>

### Additional content

You can use the **icon** prop to add an icon to the alert box.
As usual, you can provide any time of svg as a React component to the **icon** prop.

<Example
  name="Additional content" 
  height={250}
code={AdditionalCode}
embedUrl="alert--alert-with-additional-content&viewMode=story"
storybookUrl="alert--alert-with-additional-content"

>

  <Additional />
</Example>

### Additional content

You can use the **icon** prop to add an icon to the alert box.
As usual, you can provide any time of svg as a React component to the **icon** prop.

<Example
  name="Complete Alert"
  height={200}
  code={CompleteCode}
  embedUrl="alert--alert-with-all-options&viewMode=story"
  storybookUrl="alert--alert-with-all-options"
>
  <Complete />
</Example>

## Theme

To learn more about how to customize the appearance of components, please see the [Theme docs](/docs/theme).

<Theme />
