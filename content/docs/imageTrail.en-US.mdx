---
title: ImageTrail
path: imageTrail
publishedAt: 2024-02-18
githubLink: https://github.com/PolGubau/pol-ui/tree/main/src/components/ImageTrail
doc_type: component
summary: ImageTrail is a perfect component for landing pages where you want to transmit a motion and a dynamic feeling to the user. It's a component that allows you to display a set of images in a sequence, creating a trail effect while the user is hovering the component.
pageLink: https://github.com/PolGubau/pol-ui/tree/main/src/components/ImageTrail
tags:
  - imageTrail
  - React
  - Tailwind CSS
  - Pol-ui
---

import RootCode from "examples/ImageTrail/root.mdx";
import root from "examples/ImageTrail/root";

import MoreRotationCode from "examples/ImageTrail/moreRotation.mdx";
import moreRotation from "examples/ImageTrail/moreRotation";

import MoreRenderBufferCode from "examples/ImageTrail/moreRenderBuffer.mdx";
import moreRenderBuffer from "examples/ImageTrail/moreRenderBuffer";

The **ImageTrail** is a perfect component for landing pages where you want to transmit a motion and a dynamic feeling to the user. It's a component that allows you to display a set of images in a sequence, creating a trail effect while the user is hovering the component.

```jsx
import { ImageTrail } from "pol-ui";
```

## Default ImageTrail

<Example
  storybookUrl="https://pol-ui-storybook.vercel.app/?path=/story/components-imagetrail--default"
  height={480}
  name="Default"
  code={root}
  embedUrl="https://pol-ui-storybook.vercel.app/iframe.html?args=&id=components-imagetrail--default&viewMode=story"
>
  <RootCode />
</Example>

## Props

The **IconButton** component has the following props:

| Prop name         | Type                      | Default | Description                                                           |
| ----------------- | ------------------------- | ------- | --------------------------------------------------------------------- |
| images            | string[]                  | -       | An array of strings containing the URLs of the images to be displayed |
| renderImageBuffer | number                    | 50      | The amount pixels between mouse moves that will trigger a new image   |
| rotationRange     | number                    | 20      | The range of rotation in degrees that the images will randomly have   |
| className         | string                    | -       | The className of the component                                        |
| imageClassName    | string                    | -       | The className of the images                                           |
| disapearDelay     | number                    | 1       | The time in milliseconds that the images will take to disapear        |
| theme             | [ImageTrailTheme](#theme) | -       | The theme of the component                                            |

<Callout emoji="💡">
  You can also provide any prop comming from the HTML **div** tag
</Callout>

## Examples

### More rotation

To manipulate the rotation of the images, you can use the **rotationRange** prop. This prop will allow you to set the range of rotation in degrees that the images will randomly have.

<Example
  storybookUrl="https://pol-ui-storybook.vercel.app/?path=/story/components-imagetrail--more-rotation"
  height={480}
  name="More rotation"
  code={moreRotation}
  embedUrl="https://pol-ui-storybook.vercel.app/iframe.html?args=&id=components-imagetrail--more-rotation&viewMode=story"
>
  <MoreRotationCode />
</Example>
### More render buffer

To change the render buffer of the images, use the **renderImageBuffer** prop.

This prop will allow you to set the amount pixels between mouse moves that will trigger a new image.

<Example
  storybookUrl="https://pol-ui-storybook.vercel.app/?path=/story/components-imagetrail--more-render-buffer"
  height={480}
  name="More render buffer"
  code={moreRenderBuffer}
  embedUrl="https://pol-ui-storybook.vercel.app/iframe.html?args=&id=components-imagetrail--more-render-buffer&viewMode=story"
>
  <MoreRenderBufferCode />
</Example>

## Props

## Theme

```ts
interface ImageTrailTheme {
  base: string;
  image: string;
}
```
