---
title: IconButton
path: iconButton
publishedAt: 2024-02-18
githubLink: https://github.com/PolGubau/pol-ui/tree/main/src/components/IconButton
doc_type: component
summary: IconButton is a beauty way to display a action trigger without a text. A icon will be rendered with a button style.
pageLink: https://github.com/PolGubau/pol-ui/tree/main/src/components/IconButton
tags:
  - iconButton
  - React
  - Tailwind CSS
  - Pol-ui
---

import RootCode from "examples/IconButton/root.mdx";
import root from "examples/IconButton/root";

import LogoCode from "examples/Footer/logo.mdx";
import logo from "examples/Footer/logo";

import SocialCode from "examples/Footer/social.mdx";
import social from "examples/Footer/social";

import SitemapCode from "examples/Footer/sitemap.mdx";
import sitemap from "examples/Footer/sitemap";

The **IconButton** component is a beauty way to display a action trigger without a text. A icon will be rendered with a button style.

```jsx
import { IconButton } from "pol-ui";
```

## Default IconButton

<Example
  storybookUrl="https://pol-ui-storybook.vercel.app/?path=/story/components-iconbutton--default-button"
  height={170}
  name="Footer"
  code={root}
  embedUrl="https://pol-ui-storybook.vercel.app/iframe.html?args=&id=components-iconbutton--default-button&viewMode=story"
>
  <RootCode />
</Example>

## Props

The **IconButton** component has the following props:

| Prop name | Type                      | Default | Description                                 |
| --------- | ------------------------- | ------- | ------------------------------------------- |
| theme     | [IconButtonTheme](#theme) | {}      | The theme for the **IconButton** component. |

<Callout emoji="💡">
  You can also provide any prop comming from the HTML **button** tag and the
  [**Button** component props](/docs/button#theme) but the **theme** and the
  **loadingLabel** prop.
</Callout>

The concrete props for the **IconButton** component are:

```ts
export type IconButtonProps<T extends ElementType = "button"> = {
  theme?: DeepPartial<IconButtonTheme>;
} & ComponentPropsWithoutRef<T> &
  Omit<ButtonProps, "theme" | "loadingLabel">;
```

## Examples

### With Motion

The prop **hasMotion** will add a motion effect to the **IconButton** component while hovering, focusing or clicking.

<Example
  storybookUrl="https://pol-ui-storybook.vercel.app/?path=/story/components-iconbutton--default-button"
  height={170}
  name="Footer"
  code={root}
  embedUrl="https://pol-ui-storybook.vercel.app/iframe.html?args=&id=components-iconbutton--default-button&viewMode=story"
>
  <RootCode />
</Example>

## Theme

```ts
interface IconButtonTheme {
  base: string;
  color: ColorsType;
  disabled: string;
  loading: string;
  inner: {
    base: string;
    outline: string;
    color: ColorsType;
  };
  rounded: RoundedSizesTypes;
  size: MainSizesType;
}
```
