---
title: IconButton
path: iconButton
publishedAt: 2024-02-18
githubLink: https://github.com/PolGubau/pol-ui/tree/main/src/components/IconButton
doc_type: component
summary: IconButton is a beauty way to display a action trigger without a text. A icon will be rendered with a button style.
pageLink: https://github.com/PolGubau/pol-ui/tree/main/src/components/IconButton
tags:
  - iconButton
  - React
  - Tailwind CSS
  - Pol-ui
---

import RootCode from "examples/IconButton/root.mdx";
import root from "examples/IconButton/root";

import AllColorsCode from "examples/IconButton/allColors.mdx";
import allColors from "examples/IconButton/allColors";

import AllRoundedCode from "examples/IconButton/allRounded.mdx";
import allRounded from "examples/IconButton/allRounded";

import AllSizesCode from "examples/IconButton/allSizes.mdx";
import allSizes from "examples/IconButton/allSizes";

import LoadingCode from "examples/IconButton/loading.mdx";
import loading from "examples/IconButton/loading";

import DisabledCode from "examples/IconButton/disabled.mdx";
import disabled from "examples/IconButton/disabled";

import MotionCode from "examples/IconButton/motion.mdx";
import motion from "examples/IconButton/motion";

import OutlineCode from "examples/IconButton/outline.mdx";
import outline from "examples/IconButton/outline";

import WithoutTooltipCode from "examples/IconButton/withoutTooltip.mdx";
import withoutTooltip from "examples/IconButton/withoutTooltip";

The **IconButton** component is a beauty way to display a action trigger without a text. A icon will be rendered with a button style.

```jsx
import { IconButton } from "pol-ui";
```

## Default IconButton

<Example
  storybookUrl="https://pol-ui-storybook.vercel.app/?path=/story/components-iconbutton--default-button"
  height={170}
  name="Footer"
  code={root}
  embedUrl="https://pol-ui-storybook.vercel.app/iframe.html?args=&id=components-iconbutton--default-button&viewMode=story"
>
  <RootCode />
</Example>

## Props

The **IconButton** component has the following props:

| Prop name | Type                      | Default | Description                                 |
| --------- | ------------------------- | ------- | ------------------------------------------- |
| theme     | [IconButtonTheme](#theme) | {}      | The theme for the **IconButton** component. |

<Callout emoji="💡">
  You can also provide any prop comming from the HTML **button** tag and the
  [**Button** component props](/docs/button#theme) but the **theme** and the
  **loadingLabel** prop.
</Callout>

The concrete props for the **IconButton** component are:

```ts
export type IconButtonProps<T extends ElementType = "button"> = {
  theme?: DeepPartial<IconButtonTheme>;
} & ComponentPropsWithoutRef<T> &
  Omit<ButtonProps, "theme" | "loadingLabel">;
```

## Examples

### With Motion

The prop **hasMotion** will add a motion effect to the **IconButton** component while hovering, focusing or clicking.

<Example
  storybookUrl="https://pol-ui-storybook.vercel.app/?path=/story/components-iconbutton--with-motion"
  height={170}
  name="Footer"
  code={motion}
  embedUrl="https://pol-ui-storybook.vercel.app/iframe.html?args=&id=components-iconbutton--with-motion&viewMode=story"
>
  <MotionCode />
</Example>

### Outline

The prop **outline** will add a outline to the **IconButton** component.

<Example
  storybookUrl="https://pol-ui-storybook.vercel.app/?path=/story/components-iconbutton--outline-button"
  height={170}
  name="Footer"
  code={outline}
  embedUrl="https://pol-ui-storybook.vercel.app/iframe.html?args=&id=components-iconbutton--outline-button&viewMode=story"
>
  <OutlineCode />
</Example>
### Without Tooltip

To avoid having a tooltip, don't specify the **label** prop.

<Example
  storybookUrl="https://pol-ui-storybook.vercel.app/?path=/story/components-iconbutton--without-tooltip"
  height={170}
  name="Footer"
  code={withoutTooltip}
  embedUrl="https://pol-ui-storybook.vercel.app/iframe.html?args=&id=components-iconbutton--without-tooltip&viewMode=story"
>
  <WithoutTooltipCode />
</Example>

### Loading

Use the **loading** prop to set the **IconButton** component in a loading state.

<Example
  storybookUrl="https://pol-ui-storybook.vercel.app/?path=/story/components-iconbutton--loading"
  height={170}
  name="Footer"
  code={loading}
  embedUrl="https://pol-ui-storybook.vercel.app/iframe.html?args=&id=components-iconbutton--loading&viewMode=story"
>
  <LoadingCode />
</Example>

### Disabled

Use the **disabled** prop to set the **IconButton** component in a disabled state.
This will avoid the **IconButton** component to be clicked, to have a hover effect and to have a tooltip when the **label** prop is set.

<Example
  storybookUrl="https://pol-ui-storybook.vercel.app/?path=/story/components-iconbutton--disabled"
  height={170}
  name="Footer"
  code={disabled}
  embedUrl="https://pol-ui-storybook.vercel.app/iframe.html?args=&id=components-iconbutton--disabled&viewMode=story"
>
  <DisabledCode />
</Example>

### All Rounded Sizes

The **rounded** prop will set the scale of rounded corners for the **IconButton** component.

<Example
  storybookUrl="https://pol-ui-storybook.vercel.app/?path=/story/components-iconbutton--rounded"
  height={170}
  name="Footer"
  code={allRounded}
  embedUrl="https://pol-ui-storybook.vercel.app/iframe.html?args=&id=components-iconbutton--rounded&viewMode=story"
>
  <AllRoundedCode />
</Example>

### All Sizes

The **size** prop will set the scale of the **IconButton** component.

<Example
  storybookUrl="https://pol-ui-storybook.vercel.app/?path=/story/components-iconbutton--sizes"
  height={170}
  name="Footer"
  code={allSizes}
  embedUrl="https://pol-ui-storybook.vercel.app/iframe.html?args=&id=components-iconbutton--sizes&viewMode=story"
>
  <AllSizesCode />
</Example>

### All Colors

The **color** prop will set the color of the **IconButton** component.

<Example
  storybookUrl="https://pol-ui-storybook.vercel.app/?path=/story/components-iconbutton--all-colors"
  height={440}
  name="Footer"
  code={allColors}
  embedUrl="https://pol-ui-storybook.vercel.app/iframe.html?args=&id=components-iconbutton--all-colors&viewMode=story"
>
  <AllColorsCode />
</Example>

## Theme

```ts
interface IconButtonTheme {
  base: string;
  color: ColorsType;
  disabled: string;
  loading: string;
  inner: {
    base: string;
    outline: string;
    color: ColorsType;
  };
  rounded: RoundedSizesTypes;
  size: MainSizesType;
}
```
