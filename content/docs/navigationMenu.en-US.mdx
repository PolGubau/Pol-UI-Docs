---
title: NavigationMenu
path: navigation-menu
publishedAt: 2024-02-21
githubLink: https://github.com/PolGubau/pol-ui/tree/main/src/components/NavigationMenu
doc_type: component
summary: The NavigationMenu component is an important section of any website as it is the first section that appears on any page and it serves the purpose of allowing your users to navigate throughout your website.
pageLink: https://github.com/PolGubau/pol-ui/tree/main/src/components/NavigationMenu
tags:
  - navigation-menu
  - React
  - Tailwind CSS
  - Pol-ui
---

import RootCode from "examples/Navbar/root.mdx";
import root from "examples/Navbar/root";

import ClassesCode from "examples/Navbar/classes.mdx";
import classes from "examples/Navbar/classes";

import CTACode from "examples/Navbar/cta.mdx";
import cta from "examples/Navbar/cta";

import DropdownCode from "examples/Navbar/dropdown.mdx";
import dropdown from "examples/Navbar/dropdown";

import LinkClassesCode from "examples/Navbar/linkClasses.mdx";
import linkClasses from "examples/Navbar/linkClasses";

import NoHamburguerCode from "examples/Navbar/noHamburguer.mdx";
import noHamburguer from "examples/Navbar/noHamburguer";

The **Navigation Menu** is an interactive component that allows you to create a navigation list with links and dropdowns. It is a very flexible component that can be used in many different ways.

The most characteristic feature of the Navigation Menu is the ability to collapse and expand submenus with more options. In **pol-ui** we implemented a modern pattern of animations based on the **radix-ui** library to make the experience of using the Navigation Menu more pleasant.

All interactivity and options are handled by using React properties and you can customise the appearance of the navbar using the classes from Tailwind CSS.

To start using the navbar component you need to import it from Pol-ui:

```jsx
import { NavigationMenu } from "pol-ui";
```

## Default NavigationMenu

<Example
  storybookUrl="http://localhost:6006/?path=/story/components-navigationmenu--default"
  height={400}
  name="Default NavigationMenu"
  code={root}
  embedUrl="https://pol-ui-storybook.vercel.app/iframe.html?args=&id=components-navigationmenu--default&viewMode=story"
>
  <RootCode />
</Example>

## Structure

```ts
<NavigationMenu>
  // If it's a dropdown
  <NavigationMenuItem>
    <NavigationMenuTrigger></NavigationMenuTrigger>
    <NavigationMenuContent></NavigationMenuContent>
  </NavigationMenuItem>
  // If it's a link
  <NavigationMenuItem>
    <NavigationMenuLink></NavigationMenuLink>
  </NavigationMenuItem>
</NavigationMenu>
```

<Callout emoji="💡">
  NavigationMenuLink is important to ensure the correct behavior of the
  component. It is a wrapper for the anchor tag and it is responsible for the
  accessibility of the component.
</Callout>
## Props

### NavigationMenu Props

| Prop name     | Type                              | Default | Description                                                          |
| ------------- | --------------------------------- | ------- | -------------------------------------------------------------------- |
| children      | React.ReactNode                   | -       | The children of the component.                                       |
| hasIndicator  | boolean                           | true    | If true, the component will show an indicator when the menu is open. |
| listClassName | string                            | ''      | The class name of the list.                                          |
| theme         | [NavigationMenuThemeRoot](#theme) | {}      | The theme of the component.                                          |

### NavigationMenuTrigger Props

| Prop name | Type                                 | Default       | Description                      |
| --------- | ------------------------------------ | ------------- | -------------------------------- |
| children  | React.ReactNode                      | -             | The children of the component.   |
| className | string                               | ''            | The class name of the component. |
| icon      | {"FC<ComponentProps<'svg'>> "}       | TbChevronDown | The icon of the component.       |
| theme     | [NavigationMenuTriggerTheme](#theme) | {}            | The theme of the component.      |

<Callout emoji="💡">
  You can also provide any prop comming from the [HTML **button**
  tag](https://developer.mozilla.org/docs/Web/HTML/Element/button).
</Callout>

### NavigationMenuTrigger Props

| Prop name | Type                                 | Default | Description                      |
| --------- | ------------------------------------ | ------- | -------------------------------- |
| children  | React.ReactNode                      | -       | The children of the component.   |
| className | string                               | ''      | The class name of the component. |
| theme     | [NavigationMenuContentTheme](#theme) | {}      | The theme of the component.      |

## Theme

To learn more about how to customize the appearance of components, please see the [Theme docs](/docs/theming).

```ts
interface NavigationMenuTheme {
  root: NavigationMenuThemeRoot;
  trigger: NavigationMenuTriggerTheme;
  content: NavigationMenuContentTheme;
}

interface NavigationMenuThemeRoot {
  base: string;
}

interface NavigationMenuTriggerTheme {
  base: string;
  icon: string;
}
interface NavigationMenuContentTheme {
  base: string;
}
```
