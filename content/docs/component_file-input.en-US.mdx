---
title: FileInput
path: fileInput
publishedAt: 2024-02-17
githubLink: https://github.com/PolGubau/pol-ui/tree/main/src/components/FileInput
doc_type: component
summary: FileInput is a special type of input to manage files. It allows you to select one or multiple files from your device and upload them to a server.
pageLink: https://github.com/PolGubau/pol-ui/tree/main/src/components/FileInput
tags:
  - fileInput
  - React
  - Tailwind CSS
  - Pol-ui
---

import RootCode from "examples/FileInput/root.mdx";
import root from "examples/FileInput/root";

import HelperTextCode from "examples/FileInput/helperText.mdx";
import helperText from "examples/FileInput/helperText";

import AllColorsCode from "examples/FileInput/allColors.mdx";
import allColors from "examples/FileInput/allColors";

FileInput is a special type of input to manage files. It allows you to select one or multiple files from your device and upload them to a server.

To start using the component make sure that you have imported it from Pol-ui:

```jsx
import { FileInput } from "pol-ui";
```

## Default FileInput

<Example
  storybookUrl="fileinput--default-file-input"
  height={120}
  name="FileInput"
  code={root}
  embedUrl="fileinput--default-file-input&viewMode=story"
>
  <RootCode />
</Example>

## Props

| Prop name  | Type                     | Default   | Description                      |
| ---------- | ------------------------ | --------- | -------------------------------- |
| color      | Colors                   | 'primary' | The color of the FileInput       |
| helperText | ReactNode                | -         | The helper text of the FileInput |
| size       | MainSizes                | 'md'      | The size of the FileInput        |
| theme      | [FileInputTheme](#theme) | {}        | The theme of the FileInput       |

<Callout emoji="💡">
  You can also use any prop from the native **input** tag.
</Callout>

## Examples

### FileInput with a helper text

To render a Helper Text under the file input, you can use the **helperText** prop:

<Example
  storybookUrl="fileinput--helper-text"
  height={120}
  name="FileInput with a helper text"
  code={helperText}
  embedUrl="fileinput--helper-text&viewMode=story"
>
  <HelperTextCode />
</Example>
### All FileInput colors

As the **FileInput** component is a part of the **Colors** system, you can use any color with the **color** prop.

<Example
  storybookUrl="fileinput--all-colors"
  height={400}
  name="All FileInput colors"
  code={allColors}
  embedUrl="fileinput--all-colors&viewMode=story"
>
  <AllColorsCode />
</Example>

## Theme

The **DynamicHeading** doesn't have any styles by default, but you can use the **className** prop to customize the styles of the component.
