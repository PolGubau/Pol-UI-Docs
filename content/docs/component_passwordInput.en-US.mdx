---
title: PasswordInput
path: passwordInput
publishedAt: 2024-02-23
githubLink: https://github.com/PolGubau/pol-ui/tree/main/src/components/PasswordInput
doc_type: component
summary: The PasswordInput from pol-ui represents a component that allows the user to input a password. It has a toggle to show the password.
pageLink: https://github.com/PolGubau/pol-ui/tree/main/src/components/PasswordInput
tags:
  - passwordInput
  - React
  - Tailwind CSS
  - Pol-ui
---

import RootCode from "examples/PasswordInput/root.mdx";
import root from "examples/PasswordInput/root";

The **PasswordInput** from **pol-ui** represents a component that allows the user to input a password. It has a toggle to show the password.

This toggle is represented by an icon that changes the visibility of the password and it can be customized by the user.

## Importation

Import the component from **pol-ui** to use the PasswordInput element:

```jsx
import { PasswordInput } from "pol-ui";
```

## Default PasswordInput

<Example
  storybookUrl="inputs-passwordinput--default"
  height={150}
  name="Default PasswordInput"
  code={root}
  embedUrl="inputs-passwordinput--default&viewMode=story"
>
  <RootCode />
</Example>

## Props

As this component it is a **wrapper** of the [Input](/docs/input) component, so it has all the props and theme of the Input component plus the following props:
| Prop name | Type | Default | Description |
| ----------------- | ----------------------------- | -------- | --------------------------------------- |
| showIcon | {"FC<ComponentProps<'svg'>>"} | TbEye | The icon to show the password. |
| hideIcon | {"FC<ComponentProps<'svg'>>"} | TbEyeOff | The icon to hide the password. |
| defaultVisibility | boolean | false | The default visibility of the password. |

<Callout emoji="💡">
  You can also provide any prop comming from the [Input
  component](/docs/input#props).
</Callout>

## Theme

The PasswordInput component uses the [**InputTheme**](/docs/input#theme) type to customize the component.
