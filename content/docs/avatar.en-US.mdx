---
title: Avatar
path: avatar
publishedAt: 2024-02-09
githubLink: https://github.com/PolGubau/pol-ui/tree/main/src/components/Avatar
doc_type: component
summary: Use the accordion component and its options to expand and collapse the content inside each panel based on state reactivity from React and Tailwind CSS
pageLink: https://github.com/PolGubau/pol-ui/tree/main/src/components/Avatar
tags:
  - Avatar
  - React
  - Tailwind CSS
  - Pol-ui
---

import RootCode from "examples/Avatar/root.mdx";
import root from "examples/Avatar/root";

import AllSizesCode from "examples/Avatar/allSizes.mdx";
import AllSizes from "examples/Avatar/allSizes";

import AllStatusCode from "examples/Avatar/allStatus.mdx";
import AllStatus from "examples/Avatar/allStatus";

import GroupCode from "examples/Avatar/group.mdx";
import Group from "examples/Avatar/group";

import Theme from "examples/Avatar/theme.mdx";

The avatar component can be used to show a visual representation of a user or team account for your application based on multiple examples, colors, sizes and shapes.

All of the example are built as React components and you can access custom props and methods to customize the component and you can also use Tailwind CSS classes to style the component.

To start using the avatar component you need to import it from the **pol-ui** package:

```jsx
import { Avatar } from "pol-ui";
```

## Default avatar

Here's a default example of the **{"<Avatar>"}** component where you can use the **img** prop to pass the image URL, the **alt** prop to pass a description of the image for accessibility and the **rounded** prop to make the avatar rounded.

<Example
  height={150}
name="Default avatar"
code={root}
embedUrl="https://pol-ui-storybook.vercel.app/iframe.html?args=size:md&id=components-avatar--default-avatar&viewMode=story"

>

  <RootCode />
</Example>

## Props

| Prop name           | Type                                                           | Default     | Description                                                            |
| ------------------- | -------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------- |
| alt                 | string                                                         |             | The alt text for the image.                                            |
| img                 | string                                                         |             | The image URL.                                                         |
| bordered            | bool                                                           | false       | Add a border to the avatar.                                            |
| color               | Colors                                                         | 'secondary' | The color of the avatar.                                               |
| size                | MainSizes                                                      | 'md'        | The size of the avatar.                                                |
| status              | [AvatarStatus](/docs/avatar#additional-types-avatarstatusenum) | undefined   | The status of the avatar.                                              |
| stacked             | bool                                                           | false       | Stack the avatar. Useful in groups                                     |
| placeholderInitials | string                                                         | undefined   | The initials to be used as a placeholder if the image is not provided. |
| statusPosition      | Positions                                                      | 'top-left'  | The position of the status badge.                                      |
| theme               | [AvatarTheme](/docs/avatar#theme)                              | avatarTheme | The theme of the component.                                            |

## Examples

### All Avatar Sizes

You can use the **onDismiss** prop to add a close button to the alert box and a callback to be called when the alert is dismissed.

Any function can be passed to the **onDismiss** prop and it will be called when the alert is dismissed.

<Example
  height={200}
name="All Avatar Sizes"
code={AllSizes}
embedUrl="https://pol-ui-storybook.vercel.app/iframe.html?args=&id=components-avatar--all-sizes&viewMode=story"

>

  <AllSizesCode />
</Example>

### All Avatar Status

You can use the **onDismiss** prop to add a close button to the alert box and a callback to be called when the alert is dismissed.

Any function can be passed to the **onDismiss** prop and it will be called when the alert is dismissed.

<Example
  height={150}
name="All Avatar Sizes"
code={AllStatus}
embedUrl="https://pol-ui-storybook.vercel.app/iframe.html?args=&id=components-avatar--all-status&viewMode=story"

>

  <AllStatusCode />
</Example>

### Avatar group

A group of avatars can be used to show a visual representation of a user or team account for your application based on multiple examples, colors, sizes and shapes.

<Example
  height={150}
name="Group of avatars"
code={Group}
embedUrl="http://localhost:6006/iframe.html?id=components-avatar--default-avatar-group&viewMode=story"

>

  <GroupCode />
</Example>

## Theme

To learn more about how to customize the appearance of components, please see the [Theme docs](/docs/theming).

<Theme />

## References

### Additional types, AvatarStatusEnum

The **AvatarStatusEnum** is an enum that you can use to set the status of the avatar component. If you rather use a string, you can use the following values: **away**, **busy**, **offline** and **online**.

```js
export enum AvatarStatusEnum {
  away = 'away',
  busy = 'busy',
  offline = 'offline',
  online = 'online',
}

```
