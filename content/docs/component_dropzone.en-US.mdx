---
title: Dropzone
path: dropzone
publishedAt: 2024-03-02
githubLink: https://github.com/PolGubau/pol-ui/tree/main/src/components/Dropzone
doc_type: component
summary: The Dropzone displays a box where the user can drag and drop files to upload them. It also allows the user to click on the box to open the file explorer and select files to upload.

pageLink: https://github.com/PolGubau/pol-ui/tree/main/src/components/Dropzone
tags:
  - dropzone
  - React
  - Tailwind CSS
  - Pol-ui
---

import RootCode from "examples/Dropzone/root.mdx";
import root from "examples/Dropzone/root";

import AcceptCode from "examples/Dropzone/accept.mdx";
import accept from "examples/Dropzone/accept";

import SingleCode from "examples/Dropzone/single.mdx";
import single from "examples/Dropzone/single";

import CustomClassCode from "examples/Dropzone/customClass.mdx";
import customClass from "examples/Dropzone/customClass";

The **Dropzone** displays a box where the user can drag and drop files to upload them. It also allows the user to click on the box to open the _file explorer_ and select files to upload.

## Importation

Import the component from **pol-ui** to use the Timeline element:

```jsx
import { Timeline } from "pol-ui";
```

## Default Timeline

<Example
  storybookUrl="dropzone--example"
  height={380}
  name="Default Timeline"
  code={root}
  embedUrl="dropzone--example&viewMode=story"
>
  <RootCode />
</Example>

## Props

| Prop name         | Type                            | Default | Description                                                                 |
| ----------------- | ------------------------------- | ------- | --------------------------------------------------------------------------- |
| onDragStateChange | (isDragActive: boolean) => void | -       | Called when the drag state changes.                                         |
| onDrag            | () => void                      | -       | Called when the user drags a file over the dropzone.                        |
| onDragIn          | () => void                      | -       | Called when the user drags a file into the dropzone.                        |
| onDragOut         | () => void                      | -       | Called when the user drags a file out of the dropzone.                      |
| onDrop            | (e: DragEvent) => void          | -       | Called when the user drops a file into the dropzone.                        |
| onFilesDrop       | (files: File[]) => void         | -       | Called when the user drops a file into the dropzone.                        |
| multiple          | boolean                         | true    | Whether the dropzone should accept multiple files.                          |
| accept            | string                          | -       | A string that specifies the types of files that the dropzone should accept. |
| disabled          | boolean                         | false   | Whether the dropzone is disabled.                                           |
| className         | string                          | -       | A string that specifies the class name of the dropzone.                     |
| disabledClassName | string                          | -       | A string that specifies the class name of the dropzone when it is disabled. |
| activeClassName   | string                          | -       | A string that specifies the class name of the dropzone when it is active.   |
| theme             | [DropzoneTheme](#theme)         | -       | An object that specifies the theme of the dropzone.                         |

## Examples

### Single file

<Example
  storybookUrl="dropzone--single"
  height={380}
  name="Single file"
  code={single}
  embedUrl="dropzone--single&viewMode=story"
>
  <SingleCode />
</Example>

### Custom class

<Example
  storybookUrl="dropzone--custom-class"
  height={630}
  name="custom-class file"
  code={customClass}
  embedUrl="dropzone--custom-class&viewMode=story"
>
  <CustomClassCode />
</Example>
### Custom accepts

<Example
  storybookUrl="dropzone--custom-accepts"
  height={300}
  name="custom-accepts file"
  code={accept}
  embedUrl="dropzone--custom-accepts&viewMode=story"
>
  <AcceptCode />
</Example>
## Theme

To learn more about how to customize the appearance of components, please see the [Theme docs](/docs/theme).

```ts
interface DropzoneTheme {
  base: string;
  disabled: string;
  active: string;
}
```
