---
title: Button
path: button
publishedAt: 2024-02-10
githubLink: https://github.com/PolGubau/pol-ui/tree/main/src/components/Button
doc_type: component
summary: Buttons are used to trigger actions on your website. The button component from Pol-ui allows you to customise the buttons with different colors, sizes, icons, and more. All examples are built with React and Tailwind CSS.
pageLink: https://github.com/PolGubau/pol-ui/tree/main/src/components/Button
tags:
  - Button
  - React
  - Tailwind CSS
  - Pol-ui
---

import RootCode from "examples/Button/root.mdx";
import root from "examples/Button/root";

import ColorsCode from "examples/Button/colors.mdx";
import colors from "examples/Button/colors";

import ColorsOutlineCode from "examples/Button/colorsOutline.mdx";
import colorsOutline from "examples/Button/colorsOutline";

import DisabledCode from "examples/Button/disabled.mdx";
import disabled from "examples/Button/disabled";

import FullSizedCode from "examples/Button/fullSized.mdx";
import fullSized from "examples/Button/fullSized";

import IconCode from "examples/Button/icon.mdx";
import icon from "examples/Button/icon";

import LinkCode from "examples/Button/link.mdx";
import link from "examples/Button/link";

import LoadingCode from "examples/Button/loading.mdx";
import loading from "examples/Button/loading";

import MotionCode from "examples/Button/motion.mdx";
import motion from "examples/Button/motion";

import NoBackgroundCode from "examples/Button/noBackground.mdx";
import noBackground from "examples/Button/noBackground";

import OutlineCode from "examples/Button/outline.mdx";
import outline from "examples/Button/outline";

import RoundedCode from "examples/Button/rounded.mdx";
import rounded from "examples/Button/rounded";

import SizesCode from "examples/Button/sizes.mdx";
import sizes from "examples/Button/sizes";

import GroupCode from "examples/Button/group.mdx";
import group from "examples/Button/group";

## Introduction

The button component is a common UI component found on the web that allows users to trigger certain actions on your website such as submitting a form, navigating to a new page, or downloading a file.

The examples from the Pol-ui library allows you to customise the buttons with different colors, sizes, icons, and more. All examples are built with React and Tailwind CSS.

In order to start using the button component you need to import it from Pol-ui:

```jsx
import { Button } from "pol-ui";
```

## Default button

Use this example to create a default button by using the **{"<Button>"}** component from React and by adding the **color** property you can change the color of the button.

 <Example
 storybookUrl='button--default-button'
  height={100}
name="Default banner"
code={root}
embedUrl="button--default-button&viewMode=story"

>

  <RootCode />
</Example>

## Props

| Prop name       | Type              | Default       | Description                               |
| --------------- | ----------------- | ------------- | ----------------------------------------- |
| children        | React.ReactNode   | -             | The children of the breadcrumb component. |
| as              | React.ElementType | "button"      | The HTML tag to render the button as.     |
| href            | string            | -             | The URL to navigate when click.           |
| color           | Colors            | "primary"     | The color of the button.                  |
| fullSize        | boolean           | false         | The button will take the full width.      |
| loading         | boolean           | false         | The button will be in a loading state.    |
| loadingLabel    | string            | "Loading"     | The label of the loading state.           |
| hasMotion       | boolean           | false         | The button will have a motion effect.     |
| hasBackground   | boolean           | true          | The button will have a background color.  |
| loader          | React.ReactNode   | {"<Loader/>"} | The loader component to use.              |
| label           | string            | -             | The label of the button.                  |
| outline         | boolean           | false         | The button will have an outline style.    |
| rounded         | RoundedSizes      | 'md'          | The button will have a rounded style.     |
| positionInGroup | PositionInGroup   | none          | The position of the button in a group.    |
| size            | MainSizes         | "md"          | The size of the button.                   |
| theme           | ButtonTheme       | buttonTheme   | The theme of the button.                  |
| innerClassName  | string            | -             | Inner class name of the button children.  |

Add to the previous table all native props from the HTML button tag.

If you provide a **href** property, the button will render as an anchor tag, so all native props of the anchor tag are also available.

## Examples

### Outline button

Pol-ui allows you to create an outline button by using the **outline** property.
It's an alternative style for the button component.
If you rather use direct classes from Tailwind CSS, feel free to use the **className** property but this prop gives this functionality for free.

 <Example
 storybookUrl='button--outline-button'
  height={100}
name="Outline button"
code={outline}
embedUrl="button--outline-button&viewMode=story"

>

  <OutlineCode />
</Example>

### With loading state

The **loading** property allows you to create a button with a loading state. A loading spinner will be displayed and the button will be disabled.

 <Example
 storybookUrl='button--loading'
  height={100}
name="Loading button"
code={loading}
embedUrl="button--loading&viewMode=story"

>

  <LoadingCode />
</Example>

### Disabled button

To display a disabled button, you can use the **disabled** property.
This prop will change the button to a disabled state and the user won't be able to interact with it. At the same time, the button will have a different color to indicate that it's disabled.

 <Example
 storybookUrl='button--disabled'
  height={100}
name="Disabled button"
code={disabled}
embedUrl="button--disabled&viewMode=story"

>

  <DisabledCode />
</Example>

### Full with button

The **fullSize** property allows you to create a button that takes the full width of its parent container.
Useful for mobile devices or when you want to create a button that takes the full width of the screen.

 <Example
 storybookUrl='button--full-sized'
  height={100}
name="Full sized button"
code={fullSized}
embedUrl="button--full-sized&viewMode=story"

>

  <FullSizedCode />
</Example>

### Transparent button

The **hasBackground** property allows you to create a button without a background color.
This is useful when you want to create a button that looks like a link or 'just text' but still want to use the button component.

 <Example
 storybookUrl='button--transparent'
  height={100}
name="Transparent button"
code={noBackground}
embedUrl="button--transparent&viewMode=story"

>

  <NoBackgroundCode />
</Example>

### Link button

**Pol-ui** doesn't have a specific component for links, but you can use the **{"<Button>"}** component and add the **href** property to create a link button. The HTML tag will be rendered as an anchor tag just with specifying this **href** property.

 <Example
 storybookUrl='button--link'
  height={100}
name="Link button"
code={link}
embedUrl="button--link&viewMode=story"

>

  <LinkCode />
</Example>

### With Motion

If you want to add a motion effect to the button, you can use the **hasMotion** property. This will add a motion effect to the button when the user hovers over it, when the user clicks on it, or when the user focuses on it.
This is interesting when you have a fancy design and want to add a little bit of animation to the button.

 <Example
 storybookUrl='button--with-motion'
  height={100}
name="with-motion button"
code={motion}
embedUrl="button--with-motion&viewMode=story"

>

  <MotionCode />
</Example>

### All rounded styles

All rounded styles are available for the button component. You can use the **rounded** property to change the style of the button.

 <Example
 storybookUrl='button--rounded'
  height={100}
name="Rounded button"
code={rounded}
embedUrl="button--rounded&viewMode=story"

>

  <RoundedCode />
</Example>

### All sizes styles

The **size** property is used to change the size of the button. You can use the **size** property to change the size of the button.

 <Example
 storybookUrl='button--sizes'
  height={100}
name="sizes button"
code={sizes}
embedUrl="button--sizes&viewMode=story"

>

  <SizesCode />
</Example>

### All color styles

Button component follows your style guide and allows you to create buttons with different colors. You can use the **color** property to change the color of the button.

For example, you could use the color **success** when you want to create a button that indicates a successful action.

 <Example
 storybookUrl='button--all-colors'
  height={250}
name="All colors button"
code={colors}
embedUrl="button--all-colors&viewMode=story"

>

  <ColorsCode />
</Example>

### Outlined colors

Button component follows your style guide and allows you to create buttons with different colors. You can use the **color** property to change the color of the button.

For example, you could use the color **success** when you want to create a button that indicates a successful action.

 <Example
 storybookUrl='button--outline-colors'
  height={250}
name="All outlined button"
code={colorsOutline}
embedUrl="button--outline-colors&viewMode=story"
>

  <ColorsOutlineCode />
</Example>

### Button with icon

To add any icon to the button, you can use the **{"<Button>"}** component and add the **{"<Icon>"}** component as a child of the button.
To let more flexibility, Button doesn't have a specific prop to add an icon so add it as well in the children of the button.

If your **{"<Button>"}** component only has an icon, you can use the [IconButton](/icon-button) component.

 <Example
 storybookUrl='button--with-icon'
  height={100}
name="Button with icon"
code={icon}
embedUrl="button--with-icon&viewMode=story"
>

  <IconCode />
</Example>

### Button group

To create a group of buttons, you can use the **{"<ButtonGroup>"}** component and add the **{"<Button>"}** component as a child of the button group.

 <Example
 storybookUrl='buttongroup--default-avatar-group'
  height={100}
name="Button group"
code={group}
embedUrl="buttongroup--default-avatar-group&viewMode=story"
>

  <GroupCode />
</Example>

## Theme

To learn more about how to customize the appearance of components, please see the [Theme docs](/docs/theme).

```ts
interface ButtonTheme {
  base: string;
  fullSized: string;
  color: ColorsType;
  ring: {
    base: string;
    colors: ColorsType;
  };
  disabled: string;
  loading: string;
  loaderSlot: string;
  loaderLeftPosition: MainSizesType;
  inner: {
    base: string;
    position: PositionInButtonGroup;
    outline: string;
    loadingPadding: MainSizesType;
  };
  label: string;
  outline: IBoolean & {
    outlineBase: string;
    color: ColorsType;
  };
  rounded: RoundedSizesTypes;
  size: MainSizesType;
}
```
