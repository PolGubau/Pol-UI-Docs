---
title: DebouncedInput
path: debouncedInput
publishedAt: 2024-02-15
githubLink: https://github.com/PolGubau/pol-ui/tree/main/src/components/DebouncedInput
doc_type: component
summary: The DebouncedInput component is a simple input that debounces the input value to avoid unnecessary re-renders.
pageLink: https://github.com/PolGubau/pol-ui/tree/main/src/components/DebouncedInput
tags:
  - debouncedInput
  - React
  - Tailwind CSS
  - Pol-ui
---

import RootCode from "examples/DebouncedInput/root.mdx";
import root from "examples/DebouncedInput/root";

import ShortDelay from "examples/DebouncedInput/shortDelay.mdx";
import shortDelay from "examples/DebouncedInput/shortDelay";

import LongDelay from "examples/DebouncedInput/longDelay.mdx";
import longDelay from "examples/DebouncedInput/longDelay";

import ErrorCode from "examples/DebouncedInput/error.mdx";
import error from "examples/DebouncedInput/error";

The **DebouncedInput** component is a simple input that debounces the input value to avoid unnecessary re-renders.

It's also super powerful when using this input when you need to make a request to an API when the user is typing, you can reduce the number of requests by using the debounce feature.

## Importation

Import the component from **pol-ui** to use the conveyor element:

```jsx
import { DebouncedInput } from "pol-ui";
```

## Default DebouncedInput

<Example
  storybookUrl="https://pol-ui-storybook.vercel.app/?path=/story/components-inputs-debouncedinput--default"
  height={400}
  name="Default DebouncedInput"
  code={root}
  embedUrl="https://pol-ui-storybook.vercel.app/iframe.html?args=&id=components-inputs-debouncedinput--default&viewMode=story"
>
  <RootCode />
</Example>

## Props

| Prop name | Type                    | Default | Description                                              |
| --------- | ----------------------- | ------- | -------------------------------------------------------- |
| delay     | number                  | 500     | The delay in milliseconds before the value is sent.      |
| onChange  | (value: string) => void |         | The function that will be called when the value changes. |
| value     | string                  |         | The value of the input.                                  |

<Callout emoji="💡">
  You can also sent any prop available in the [Input
  component](/docs/input#theme) to the DebouncedInput.
</Callout>
## Examples

### Custom delay

To change the delay of the DebouncedInput, you can use the delay prop to set the delay in milliseconds.

#### Short delay

For example, you can set the delay to 10ms to send the value as soon as the user stops typing.

<Example
  storybookUrl="https://pol-ui-storybook.vercel.app/?path=/story/components-inputs-debouncedinput--less-delay"
  height={400}
  name="Short delay"
  code={shortDelay}
  embedUrl="https://pol-ui-storybook.vercel.app/iframe.html?args=&id=components-inputs-debouncedinput--less-delay&viewMode=story"
>
  <ShortDelay />
</Example>

#### Long delay

If you need to wait a little bit more before sending the value, you can also set a longer delay.

<Example
  storybookUrl="https://pol-ui-storybook.vercel.app/?path=/story/components-inputs-debouncedinput--long-delay"
  height={400}
  name="Long delay"
  code={longDelay}
  embedUrl="https://pol-ui-storybook.vercel.app/iframe.html?args=&id=components-inputs-debouncedinput--long-delay&viewMode=story"
>
  <LongDelay />
</Example>

### With error

As well with the Input component, you can also use the DebouncedInput with the error prop and a helperText to show an error message.

<Example
  storybookUrl="https://pol-ui-storybook.vercel.app/?path=/story/components-inputs-debouncedinput--with-error"
  height={400}
  name="Long delay"
  code={error}
  embedUrl="https://pol-ui-storybook.vercel.app/iframe.html?args=&id=components-inputs-debouncedinput--with-error&viewMode=story"
>
  <ErrorCode />
</Example>

## Theme

The **DebouncedInput** component uses the same theme as the [Input component](/docs/input#theme).
