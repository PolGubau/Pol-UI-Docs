```jsx
import {Popover, PopoverTrigger, PopoverContent } from "pol-ui";

constPopoverCardComponent = () => {
  return (
    <Popover>
      <PopoverTrigger>
        <Button>Open Popover</Button>
      </PopoverTrigger>
      <PopoverContent>Content of the popover</PopoverContent>
    </Popover>
  );
};
export defaultPopoverCardComponent;
```
