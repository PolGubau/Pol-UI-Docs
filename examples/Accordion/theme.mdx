```ts
interface AccordionTheme {
  root: {
    base: string;
    isBordered: IBoolean;
  };
  content: {
    base: string;
  };
  title: {
    arrow: {
      base: string;
      open: IBoolean;
    };
    base: string;
    isBordered: IBoolean;
    heading: string;
    open: IBoolean;
  };
}
```
